# The eval suites run from the repo root (`python -m pytest evals -q`), where
# backend/pyproject.toml's pytest settings do not apply. asyncio_mode=auto lets the
# async driver tests run without a per-test marker, matching the backend's setting.
[pytest]
asyncio_mode = auto
